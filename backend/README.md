# askless backend

## Setup

1. Create a `.env` file in this folder using `.env.example` as a template.
2. Ensure Node.js 20+.
3. Install deps and start the server.

```bash
npm install
npm run dev
```

The server runs on `http://localhost:4000` and exposes `POST /api/answer`.
